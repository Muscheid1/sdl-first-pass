{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "build",
            "command": "make -f makefile.mac",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "Builds our program using the exisitng Makefile."
        }
    ],
    "version": "2.0.0"
}